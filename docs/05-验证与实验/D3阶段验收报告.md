# D3阶段验收报告

版本：1.0.0  
状态：通过（用户完整环境验收）  
阶段：D3自动施压与安全控制数字仿真

## 1. 已完成范围

D3-A至D3-F已分别完成契约、被控对象、PID、安全状态机、故障矩阵、报告/API和Web时间线。D3-G新增被控对象—控制器—设备状态机真实集成运行以及30分钟模型时长回归。

## 2. 正常Profile集成结果

默认Profile为20、40、60 `force_au`，最终状态为IDLE，总模型时间约10.27秒。

| 目标 | 稳定时间 | 稳态误差 | 超调 | 模型阈值 |
|---:|---:|---:|---:|---|
| 20 au | 1.65 s | 0.696 au | 3.56% | 通过 |
| 40 au | 1.59 s | 0.785 au | 4.00% | 通过 |
| 60 au | 1.56 s | 0.759 au | 3.87% | 通过 |

阈值：稳定时间不超过3秒、稳态误差不超过2 au、超调不超过10%。

状态时间线覆盖APPROACH、CONTACT、STABILIZE、ACQUIRE、STEP、RETRACT和IDLE，相同seed与配置报告哈希一致。

## 3. 30分钟模型时长结果

- 模型时长：1800秒；
- 数值状态：全部有限；
- 最大载荷：约41.462 au；
- 最大位置：约19.470 au；
- 最大积分贡献：约0.128，低于1.0上限；
- 最终载荷误差：约 `4.3e-14 au`；
- 无无界积分或非有限状态。

## 4. 已有安全证据

- 急停立即零输出并锁存，不虚构主动退回；
- ABORT和主机失联由设备侧受控退回；
- 硬超限、上下限位、传感器失效、卡滞和看门狗进入冻结安全态；
- 同tick多故障按冻结优先级裁决并保留完整检测集合；
- 14类故障矩阵可确定性重放；
- 报告持久化后读取校验SHA-256；
- Web只展示和请求实验，不直接写电机输出或清除锁存故障。

## 5. 最终验收结果

```bash
+python -m pytest tests/test_d3_integration.py -q
+python -m pytest -q
+cd web
+npm run build
+```

用户已在完整仓库环境确认：

- D3-G专项测试通过；
- Python全量回归通过；
- Web生产构建通过；
- 浏览器D3矩阵、14条安全事件、确定性重放通过；
- D0、D2原有入口无回归。

本报告据此签署为“通过”。具体测试数量未由用户提供，因此不填写推测数字，也不表述为GitHub Actions CI。

## 6. 证据边界

本报告证明数字模型和软件安全逻辑在冻结配置下的行为。它不证明：

- 真实执行器推力、制动、卡滞和断电释放；
- 真实传感器量程、漂移、失效诊断或限位可靠性；
- 真实结构强度、接触面积、夹伤风险或人体安全载荷；
- 任何临床、诊断、脉象或治疗有效性。

因此D3通过后只允许进入H1采购与台架验证，不允许直接进入人体自动施压实验。
